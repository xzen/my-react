import React from 'react';

import Navbar from './nav-bar';

const Header = () => (
  <div className="row">
    <div className="col-12">
      <Navbar />
    </div>
  </div>
);

export default Header;
